"""core — cross-cutting concerns shared by every agent.

Modules:
    auth              — Google OAuth flow (returns reusable Credentials)
    claude_client     — Anthropic client setup + cost reporting helpers
    knowledge_base    — load Baldwin County reference + Chad comm rules
"""
